.input {
  box-sizing: border-box;
  margin: 0;
  padding: 9px 16px;

  height: 32px;
  width: 480px;

  background: var(--color-white-base);
  color: var(--color-text-base);
  font-size: 14px;

  border: 1px solid var(--color-grey-additional);
  box-shadow: inset 0px 1px 2px rgba(0, 0, 0, 0.1);
  border-radius: 4px;
  outline: none;
}